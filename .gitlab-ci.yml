image: python:3.7-alpine

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
    - pip install -U sphinx recommonmark
    - sphinx-build -b html docs/source public
  artifacts:
    paths:
      - public
  only:
    - master
